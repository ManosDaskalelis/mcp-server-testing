# Demo MCP Server (.NET 10)

MCP Server using **HTTP transport** - runs as a web server and exposes the MCP endpoint at `/mcp`. No authentication. No Dockerfile.

## Testing

Test it in MCP Inspector:

http://localhost:8080/mcp
